<div class="title">
    <h2>Categories</h2>
</div>
<div class="content">
    <ul>
      <% Category.all.each do |category| %>
          <li> <%= link_to category.name, collections_path + "/byCategory/" + category.name %></li>
      <% end %>
    </ul>
</div>

